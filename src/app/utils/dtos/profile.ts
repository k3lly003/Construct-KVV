export interface profileProps {
  NK: string;
  userName: string;
  userEmail: string;
  profilePic?: string;
}

export interface UserData {
  firstName: string;
  lastName: string;
  email: string;
  phone: string;
  profilePic?: string;
  role?: string;
}
